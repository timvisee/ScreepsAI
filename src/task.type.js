// Variable used to determine task type indices
var i = 0;

/**
 * List of task types.
 *
 * @type {*}
 */
module.exports = {

    /**
     * Task to harvest energy.
     */
    TASK_HARVEST_ENERGY: {
        id: i++
    }
};