// Variable used to determine action type indices
var i = 0;

/**
 * List of action types.
 *
 * @type {*}
 */
module.exports = {

    /**
     * Action to move to a specified point.
     */
    ACTION_MOVE_TO: {
        id: i++,
        file: 'moveTo'
    }
};